package com.mfashiongallery.emag.app.view;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.FrameLayout;

/**
 * Created by liuchonghui on 2017/2/16.
 */
public class LockWallpaperPreview extends FrameLayout {
    public LockWallpaperPreview(Context context) {
        super(context);
    }

    public LockWallpaperPreview(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public LockWallpaperPreview(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
